query Board($id: ID!) {
  board(id: $id) {
    id
    title
    description
    permissions {
      createTask
      updateTask
      deleteTask
      moveCard
      moveColumn
      createColumn
      manageLabels
      inviteMember
      manageBoardMembers
    }
    visibility
    createdAt
    updatedAt
  }
}

